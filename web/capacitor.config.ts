import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shortvedio.interaction',
  appName: '短剧互动',
  webDir: 'dist',
  android: {
    backgroundColor: '#000000',
  },
};

export default config;
